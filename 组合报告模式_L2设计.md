# 组合报告模式（源序组合 / Source-order Composition）设计规格

> 状态：草案 v2（待用户审阅拍板）
> 作者：大湾区靓仔（项目总监视角）
> 日期：2026-07-27
> 关联：舆情分析七段式接入（已完成）、技能家族统一落位（已完成）
> 版本沿革：v1 = L2 声明式（meta-block 单选扩展）；v2 = **源序组合**（本版，应"政策必须纳入 + 任意子集自由组合"诉求重写）

---

## 1. 背景与目标

当前渲染器（`docx_renderer.py` 128–154 行）用 `if / elif / else` 链**只选一种**报告模式的 `section_order` 去渲染，未被选中的章节在 155 行循环里被 `if section is None: continue` **静默丢弃**。

两个痛点：
1. **一次只能出一种领域的报告**（政策 / 事件 / 舆情 / 组织 四选一）。
2. **混写会静默丢段**——最危险，看起来跑成功实则少了几节，且无报错。

用户的真实诉求（原话）：**"搭配方式可能灵活多变"**——列举了 舆情+组织、事件+政策、舆情+政策、舆情+组织+政策、**事件+舆情+组织+政策** 等，并明确要求**政策必须纳入**。

本规格提出 **源序组合（Source-order Composition）** 方案：
- **作者按什么顺序写，引擎就按什么顺序渲染；识别到哪几个模块，就渲染哪几个。**
- **不预定义任何组合**——15 种非空组合（含全四合一、含政策+事件）一律不特殊对待。

**设计原则**：
- **向后完全兼容**：单模式报告输出与今天**逐字一致**（零回归）。
- **碰撞确定性**：所有键冲突用"有序列表 + 共享键规则"化解，绝不静默覆盖。
- **最小侵入**：编号、目录、DIAGRAM 计数器本就统一顺序/统一收集，改用有序列表后**自动正确，这三块零改动**。

---

## 2. 核心洞察（瓶颈在哪）

> **解析器已经能收多域段落**——只要各家的 canonical ID 不同（`opinion_event` / `case_portrait` / `org_portrait` 互不冲突），`parser.py` 会把它们**全部**存进 `report.sections` 字典，一个不落。
> **真正的死穴只在渲染器那一段 `if/elif`**：它拿到一把齐全的段落，却只挑一种顺序渲染、把别的丢了。

所以"灵活化"的主力战场是**渲染器模型**，解析器只需从"字典"升为"有序列表"以支持同名段并存。

---

## 3. 真实碰撞面（已读源码核对）

把 `parser.py` 的 `_SECTION_IDS` 与 `docx_renderer.py` 路由全翻过，结论：四个模式里**只有 `政策` 与 `事件` 在三个键上撞车，其余全部互不重叠**。

| 模式 | 独有键 | 共享键（全局收尾 / 引言） |
|---|---|---|
| 政策 | `policy_portrait` `policy_weight` | `fact_summary` `framework` `analysis_body` `conclusion` `appendix` |
| 事件 | `case_portrait` `case_flows` `case_dynamics` | 同上 |
| 舆情 | 全部 `opinion_*` | `conclusion` `appendix` |
| 组织 | 全部 `org_*` | `conclusion` `appendix` |

推论：
- **舆情+组织 / 舆情+政策 / 事件+组织 / 事件+舆情+组织** 等——命名空间完全不碰，**今天就能干净渲染**（只要去掉 `elif` 单选）。
- 唯一的真·碰撞是 **政策+事件** 共享 `事实摘要` / `分析框架` / `三元结构分析正文` 三件套。
- `结论` / `附录` 是所有模式共用的全局收尾。

---

## 4. 数据模型变更（parser.py）

### 4.1 `Section` 增加归属字段

```python
@dataclass
class Section:
    title: str
    cid: str                       # canonical id，如 fact_summary（新增）
    mode: Optional[str]            # 归属模式：policy/event/org/opinion/None(共享)（新增）
    blocks: list[Block] = field(default_factory=list)
```

### 4.2 `ParsedReport` 增加有序列表（取代字典覆盖）

```python
@dataclass
class ParsedReport:
    title: str
    section_seq: list[Section] = field(default_factory=list)  # ← 取代 sections 字典，按出现顺序
    sections: dict = field(default_factory=dict)                # 兼容视图（last-wins），engine 旧代码不崩
```

### 4.3 解析状态机改写（原 271–273 行）

```python
# 旧
current_section = Section(title=sec_title)
sections[sec_id] = current_section
# 新
current_section = Section(title=sec_title, cid=sec_id, mode=_mode_of(sec_id))
report.section_seq.append(current_section)   # 有序列表：同名段不再互相覆盖
sections[sec_id] = current_section          # 兼容视图（last-wins），旧代码不崩
```

> `_mode_of(cid)` 查 `MODULES` 注册表返回归属模式；共享键返回 `None`。
> **关键收益**：政策+事件的两个 `事实摘要` 现在都是 `section_seq` 里的独立项，**不再互相覆盖**。

---

## 5. MODULES 注册表（数据驱动，取代硬编码 if/elif）

把写死的 `if/elif` 分支抽成声明式表。**新增任意模式 = 往表里加一项，渲染器零改动**——这是"灵活多变"能成立的根。

```python
# 示例，非指定——完整章节 ID 由现有 _SECTION_IDS 扩展而来
MODULES = {
    "policy": {
        "sections": ["fact_summary", "framework", "policy_portrait",
                      "policy_weight", "analysis_body", "conclusion", "appendix"],
        "sentinels": ["policy_portrait", "policy_weight"],
        "label": "政策",
    },
    "event": {
        "sections": ["fact_summary", "framework", "case_portrait",
                      "case_flows", "analysis_body", "case_dynamics",
                      "conclusion", "appendix"],
        "sentinels": ["case_portrait", "case_flows", "case_dynamics"],
        "label": "事件/案例",
    },
    "org": {
        "sections": ["org_portrait", "org_structure", "org_survival",
                      "org_reproduction", "org_interest_network", "org_reverse",
                      "org_transformation", "conclusion", "appendix"],
        "sentinels": ["org_portrait", "org_structure", ...],
        "label": "组织",
    },
    "opinion": {
        "sections": ["opinion_event", "opinion_actors", "opinion_narrative",
                      "opinion_trilife", "opinion_reverse", "opinion_evolution",
                      "conclusion", "appendix"],
        "sentinels": ["opinion_event", "opinion_actors", ...],
        "label": "舆情",
    },
}
# 共享键（全局单例/可重复，见 §6）
SHARED_KEYS = ["fact_summary", "framework", "analysis_body", "conclusion", "appendix"]
```

---

## 6. 路由算法（取代 if/elif，源序渲染）

### 6.1 检测出现过的模式

```python
present = [m for m in MODULES
           if any(s in report._cid_index for s in MODULES[m]["sentinels"])]
```
（`report._cid_index` 为 `section_seq` 里所有 cid 的集合，解析时顺手建。）

### 6.2 渲染顺序决策

```python
if len(present) <= 1:
    # 单模式：按该模式 canonical 顺序渲染 = 与今天输出逐字一致（零回归）
    order = [s for s in MODULES[present[0]]["sections"]
             if s in report._cid_index] if present else \
            [s for s in ["fact_summary","framework","analysis_body","conclusion","appendix"]
             if s in report._cid_index]
else:
    # 多模式：按作者源序渲染（你要的"灵活多变"）
    order = [sec.cid for sec in report.section_seq]
```

### 6.3 渲染循环改写（原 128–164 行整段替换）

```python
# 旧：if/elif 链 → 选定 section_order → enumerate(section_order) → report.sections.get(sec_id)
# 新：直接遍历有序段落列表
for section_number, sec in enumerate(report.section_seq, start=1):
    if sec is None:
        continue
    _render_section(doc, sec, config, sec.cid,
                    section_number=section_number,
                    output_folder=output_folder,
                    diagram_collector=diagram_collector,
                    diagram_counter=_diagram_counter)
```

> **`if/elif` 整个消失。** 编号（155 行 `enumerate`）、目录（120 行扫 H1）、DIAGRAM 计数器（125 行 `_diagram_counter`）**本来就是按统一顺序跑的，一行不用动**。

### 6.4 单模式零回归说明

单模式时 `order` 取 canonical 序（如政策：事实摘要→框架→政策肖像→政策权重→分析正文→结论→附录），与今天 `docx_renderer.py` 128–154 行为一致。AI 按提示词模板也是 canonical 序写，源序与 canonical 序天然吻合，双保险。

---

## 7. 共享键与碰撞规则（确定性，不靠猜）

### 7.1 全局收尾：`结论` / `附录`

- **默认可重复**：作者写几个就渲染几个。写"事件后一个小结 + 舆情后一个小结 + 最后总论"都合法。
- 单模式时作者通常只写一个（= 今天行为）。

### 7.2 共享引言三件套（政策 + 事件 的独家碰撞）

`fact_summary` / `framework` / `analysis_body` 定为**报告级可重复段**：

- 作者写一次 `## 事实摘要` → 渲染一次，作为跨模式共享引言。
- 作者写两次（如 `## 事实摘要（政策）` / `## 事实摘要（事件）`）→ 两段都渲染，各自独立。
- **不再有"后写覆盖先写"**——因为底层是有序列表，同名段并存。

> 这条规则把"政策+事件"这对最别扭的组合也化解为普通可重复段，**无需报错、无需强制限定**，作者完全掌控。

### 7.3 未注册章节

稿中出现注册表外的章节 ID（用户的自定义段落）→ **照样渲染**（不丢），但在报告中标记"未注册章节：XXX"，便于发现笔误。

---

## 8. 可选 meta-block 声明（增强，非必须）

无声明块时纯靠哨兵自动识别，**向后完全兼容**。可选声明仅用于校验/显式编排：

```markdown
<!-- report:
  modes: [event, opinion, org, policy]   # 可选，仅做校验
  compose: source | volume             # source=作者顺序(默认) | volume=各模式canonical拼接
  conclusion: shared | per-mode       # shared=一份总结论(默认) | 各家各写
-->
```

- 无 `<!-- report: -->` 块 → 走自动识别，四种子模式一行不改。
- `compose: volume` 时把各模式 canonical 序拼接（而非源序），满足"先完整展开事件卷、再完整展开舆情卷"的诉求。

---

## 9. 兼容性矩阵（全部 15 种非空组合，无特殊分支）

| 组合 | 是否干净 | 机制 |
|---|---|---|
| 事件+舆情 / 舆情+组织 / 事件+组织 / 舆情+政策 | ✅ 命名空间不碰 | 源序渲染 |
| 事件+舆情+组织 / 舆情+组织+政策 / 事件+舆情+政策 / 任意三合一 | ✅ | 源序渲染 |
| **事件+政策** | ✅ 仅共享三件套 | 写一次=共享引言；写两次=两段 |
| **事件+舆情+组织+政策（全四合一）** | ✅ | 源序渲染 + 共享引言一次 |
| 任一单模式（政策/事件/舆情/组织） | ✅ 零回归 | canonical 顺序 |

---

## 10. 组合提示词模板草稿（analysis_prompt.md 新增章节）

在 `analysis_prompt.md` 末尾新增「组合报告（任意子集）提示词模板」，核心约束交给 AI：

```
当你需要就同一对象输出多视角组合报告时，直接按源序书写各模式的规范章节即可，
引擎会自动识别并依次渲染。无需任何特殊标记。

章节使用规范：
1. 每个模式的章节 ID 严格使用对应模式的规范 ID（见各模式模板），不要自造。
2. 事件/舆情/组织 三家章节 ID 互不重叠，直接写即可；政策与事件共享
   「事实摘要 / 分析框架 / 三元结构分析正文」三件套——写一次即共享引言，
   写两次（如加（政策）/（事件）限定）即各成一段。
3. 结论与附录：全篇可只写一个总结论，也可各模式后各写一个小结，最后再总论。
4. DIAGRAM 按各模式原有规则使用，计数器自动不重名。
5. 顺序由你决定：写「舆情→事件→组织」就那个序，写「事件→舆情」也行。
```

---

## 11. 改动清单（精确行号 + 影响面）

| 文件 | 改动 | 风险 |
|------|------|------|
| `parser.py` | ① `Section` 加 `cid`/`mode` 字段；② `ParsedReport` 加 `section_seq` 有序列表 + `_cid_index`；③ 271–273 行 `sections[sec_id]=` 改为 `append` 并保留 `sections` 兼容视图；④ 新增 `_mode_of()` 查 `MODULES` | 中（核心解析，需测试覆盖） |
| `docx_renderer.py` | 128–164 行 `if/elif` + `enumerate(section_order)` 整段替换为 `enumerate(report.section_seq)` 顺序渲染；新增 `MODULES` 注册表 + `present` 检测 | 低 |
| `analysis_prompt.md` | 新增「组合报告（任意子集）」提示词模板 | 低 |
| `AGENTS.md` | 报告类型表增加"组合（源序）"行 + 共享键说明 | 低 |
| `tests/` | 新增组合报告测试：event+opinion+org+policy 全四合一跑通，校验连续编号 / 目录 / DIAGRAM 不重名；反例：policy+event 双写 `事实摘要` 不丢段 | 低 |

**不动**：编号逻辑、目录扫描、DIAGRAM 计数器、四种单模式报告输出内容、`engine.py` 出口、`theory_config.json`。

---

## 12. 验证计划

1. 写一份真实感样例 Markdown：含 `opinion_event`→`case_portrait`→`org_portrait`→`policy_portrait` 四模式代表章节 + 1 张 DIAGRAM，按"舆情→事件→组织→政策"源序写。
2. 跑 `CaseAnalysisEngine.export_from_text(...)`，检查：
   - 章节按源序连续中文编号（一、二、三…）
   - 目录含全部章节、无遗漏
   - DIAGRAM 文件名 `图1` 递增不重名
3. 反例测试：policy+event 各写一份 `事实摘要（政策）`/`事实摘要（事件）` → 预期两段都渲染、不丢。
4. 兼容测试：无声明块的老舆情稿 → 行为与今天一致（逐字）。

---

## 13. 决策收口（已确认 / 已定）

> 用户拍板（2026-07-27）：**"别动老东西"** —— 单模式行为与今天逐字一致；其余按项目总监建议收口。

**✅ 已确认（原第 1 条）**
- 单模式时**强制 canonical 序**渲染（与今天输出逐字一致，零回归）；仅多模式才按作者源序。这是"别动老东西"的硬保证。

**📌 已定：延后（原第 2/3/4 条，首版不做）**
- **per-mode 结论开关**：不单列。方案默认"结论可重复写"已��然支持 per-mode，加开关是画蛇添足。
- **`compose: volume` 分卷拼接**：延后。默认源序已覆盖全部 15 种组合，volume 作 v2.1 增强。
- **`<!-- report: -->` meta-block 声明**：延后。纯靠哨兵自动识别已覆盖所有组合，meta-block 仅作校验/显式编排增强，v2.1 再加。

> **首版范围锁定（最小版）**：§4（parser 数据模型）+ §5（MODULES 注册表）+ §6.3（渲染器源序）+ §11 的 parser/docx 两项改动。提示词模板与测试随实现补。

---

> 本规格已对齐真实代码（`parser.py` 271–273 行、`docx_renderer.py` 128–164 行）。确认后即进入 Phase 3 实现，先落地"最小版"（§4 + §5 + §6.3 + §11 的 parser/docx 两项），跑通全四合一样例再补提示词与测试。
