# 案例参考素材（_materials）

本目录存放来自「三元结构理论 分析skill脚本程序 - 副本」的 7 个案例脚本。
它们依赖已废弃的 `typed_report` 强类型模块（旧快照，后端未采用），
**当前不可直接运行**，仅作为案例材料/结构参考保留。

可运行案例请见上一级 `cases/` 目录（run_report.py 等，基于 engine.CaseAnalysisEngine.export_from_text）。
