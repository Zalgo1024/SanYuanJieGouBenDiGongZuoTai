# 分析skill 自包含化 — 具体工作计划（2026-07-31）

> 目标一句话：把分析skill 从"只有内核"变成"内核+前端+后端+一键启动"的完整项目，双击分析skill 里的 start.bat 就能用整套系统。
> 依据：requirements-2026-07-31-v2.md（用户需求）+ Codex 评审（需解决的真问题）。

---

## 现状盘点（动手前先认清）

**A. 分析skill 目录（今天要改的就是它）**
```
三元结构理论 分析skill脚本程序/
├── engine.py / parser.py / docx_renderer.py / viz_network.py / config.py / theory_config.json   ← 内核（成熟，不动核心逻辑）
├── backend/                          ← 旧孤儿适配器 1347 行（Codex 审的就是它，废弃）
├── frontend/                         ← 只剩 .next 编译快照（没有源码）
├── tests/test_heading_numbering.py   ← 有 1 个失败的测试
└── AGENTS.md / analysis_prompt.md
```

**B. Web 分析模型 目录（成果所在，要搬进来）**
```
Web 分析模型/
├── frontend/   ← 前端源码（Next.js：app/components/lib/package.json 等，今天升级过 commit 6306ef0）
├── backend/    ← 新后端 5244 行（FastAPI，今天升级过 commit ceb3e30，含 search/materials/5类型/版本管理）
├── start.bat   ← 一键启动脚本
└── backend/app/settings.py 的 ENGINE_DIR 现在指向分析skill 内核
```

---

## 工作计划（10 步，按顺序执行）

### 第 1 步：盘点要搬的文件（只读，0.5h）
- 列出 Web 分析模型/frontend 要搬的源码文件（排除 node_modules、.next、.npm-cache、.env*.local、日志）
- 列出 Web 分析模型/backend 要搬的文件（排除 .venv、runtime、generated、__pycache__）
- 产出：搬迁清单（记录在 .workbuddy/artifacts/move-manifest.md）
- 完成标准：清单列全，无遗漏

### 第 2 步：给分析skill 里的旧东西让位（归档不删，0.5h）
- `backend/`（旧孤儿）→ 移到 `_archived/backend_orphan/`
- `frontend/`（残留快照）→ 移到 `_archived/frontend_stale/`
- 产出：分析skill 根目录干净，腾出 backend/ 和 frontend/ 两个位置
- 完成标准：旧文件原样保留在 _archived/，一个不删

### 第 3 步：把成果搬进分析skill（2h）
- Web 分析模型/frontend 源码 → 分析skill/frontend/
- Web 分析模型/backend 源码 → 分析skill/backend/
- Web 分析模型/start.bat → 分析skill/start.bat（改内部路径，见第 4 步）
- Web 分析模型/backend 的 .env.example、requirements.txt → 分析skill/backend/
- 产出：分析skill 里有了完整的前后端源码（git 可追踪）
- 完成标准：ls 检查目录结构与 Web 分析模型一致

### 第 4 步：改配置，让前后端在同仓跑通（1h）
- `backend/app/settings.py`：`ENGINE_DIR` 从"指向 Web 分析模型"改为"指向分析skill 项目根"（相对自身，不再写死隔壁路径）
- `start.bat`：路径改成分析skill 自己的（后端 backend、前端 frontend、打开 127.0.0.1:3000/dashboard）
- CORS 核对：仍只放行 127.0.0.1:3000 / localhost:3000
- 产出：双击分析skill/start.bat 的逻辑成立
- 完成标准：settings.py 与 start.bat 里不再出现 Web 分析模型 路径

### 第 5 步：装依赖（1h）
- 后端：分析skill/backend 下建 .venv + `pip install -r requirements.txt`（含 trafilatura）
- 前端：分析skill/frontend 下 `npm install`（node_modules 装好）
- 产出：两个环境就绪
- 完成标准：import 测试 / npm 无报错

### 第 6 步：同仓启动验证（2h）
- 起后端 uvicorn（127.0.0.1:8000）→ `/api/health` 200
- 起前端 next（127.0.0.1:3000）→ dashboard 200
- 跑核心链路：`GET /api/tasks` 有数据；5 类型各提交一次 analyze 到 done；联网写报告一篇（DDG 检索→抓取→生成）；`GET /api/cases` 返回 12 篇；revise+rollback 版本 +1 可回滚
- 产出：分析skill 同仓验证通过（沙箱用 GENERATED_DIR 绕行 safe-delete，真机不用）
- 完成标准：上述接口/流程全通

### 第 7 步：修 Codex 戳中的真问题（3h）
1. **任务恢复机制**（Codex 说最狠的一条）：后端 queue 启动时扫描数据库里 pending/running 的任务，自动恢复续跑，不再"崩溃即丢"
2. **修内核失败的测试**：test_heading_numbering.py 那个"章节编号"失败的用例，定位原因（是测试错还是渲染错）→ 修到全绿
3. **测试全绿**：内核 tests + 新后端 tests 全部通过（孤儿层的 e2e_test 已归档，不再算数）
- 产出：测试全绿、任务不丢
- 完成标准：`pytest` 内核+后端全过；模拟 kill 进程后重启，pending 任务能续跑

### 第 8 步：git 入库（0.5h）
- 更新分析skill/.gitignore（backend/.venv、runtime、generated、node_modules、.next、_archived、reports 等产物忽略）
- git add + commit：第一条"分析skill 完整项目"提交
- 产出：整个项目 git 可追踪、可回滚
- 完成标准：git status 干净，commit 含前后端+内核

### 第 9 步：更新文档（0.5h）
- AGENTS.md：改成新结构说明（分析skill = 内核+应用 完整项目，一键启动方式）
- 产出：任何人打开项目 5 分钟看懂怎么跑
- 完成标准：AGENTS.md 与实际结构一致

### 第 10 步：真机验收（用户操作 + 我配合）
- 你双击 **分析skill** 的 start.bat，按验收清单走：
  ① 界面打开、任务列表有数据
  ② 丢关键词生成 1 篇（来源≥3、附录可点击、Word/PDF 可下载）
  ③ 丢链接生成 1 篇（抓正文成稿）
  ④ 5 类型各 1 篇（选的=出的）
  ⑤ 展览页手动改+AI 改 → 版本时间线≥3 版 → 回滚成功
  ⑥ 案例库预览/套用/编辑
- 产出：验收通过；哪步不对 → 记录现象，我拉人修
- 完成标准：第 5 节六条全过

---

## 改动文件清单（关键路径汇总）

| 位置 | 动作 |
|---|---|
| 分析skill/frontend/ | 新放入前端源码（替代残留快照） |
| 分析skill/backend/ | 新放入后端源码（替代旧孤儿） |
| 分析skill/start.bat | 新增（从 Web 分析模型 改造） |
| 分析skill/backend/app/settings.py | ENGINE_DIR 改为自身项目根 |
| 分析skill/_archived/ | 旧孤儿归档（backend_orphan/ frontend_stale/） |
| 分析skill/.gitignore | 补产物忽略 |
| 分析skill/AGENTS.md | 结构说明更新 |
| 分析skill/backend/app/queue.py | 加任务恢复机制 |
| 分析skill/tests/test_heading_numbering.py | 修失败用例（或修对应内核渲染 bug） |

## 红线（不变）
- 内核 engine/parser/docx_renderer/viz_network 核心逻辑不动（只修测试指出的 bug，不动架构）
- 旧文件归档不删
- 改完的每步都有验收点，不黑箱
