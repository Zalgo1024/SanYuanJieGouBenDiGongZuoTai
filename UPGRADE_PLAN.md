# 三元结构理论分析系统 — 报告质量升级方案

> 版本：v1.0
> 日期：2026-06-28
> 状态：方案整理，待实施
> 说明：本方案仅整理升级方向和具体措施，不包含代码实现。

---

## 一、问题总览与优先级

| 编号 | 问题 | 影响程度 | 修复难度 | 优先级 |
|---|---|---|---|---|
| P0 | 附录链接格式违规（非可点击超链接） | 高（影响专业性） | 低 | **先修** |
| P0 | 结论部分与前面分析内容重复 | 高（影响阅读体验） | 低 | **先修** |
| P1 | 缺乏数据锚点，判断缺少可核验支撑 | 中高 | 中 | 第二批 |
| P1 | 博弈推演为描述性，非负阈值推演 | 中高 | 高 | 第二批 |
| P1 | 应对方向过于通用，缺少针对性 | 中高 | 中 | 第二批 |
| P2 | 部分段落过长，阅读节奏不佳 | 中 | 低 | 第三批 |
| P2 | 图表与正文叙事分工不清晰 | 中 | 中 | 第三批 |
| P3 | AGENTS.md 案例清单未同步更新 | 低（内部管理问题） | 低 | 顺手修 |

---

## 二、P0 问题修复方案

### 2.1 附录链接格式违规

**现状**：附录来源以纯文字形式输出，无法在 Word/PDF 中点击跳转。

**根因**：`analysis_prompt.md` 里的附录写作规范没有明确强制 `[名称](url)` 格式；parser 解析附录段落时也没有做链接提取和校验。

**修复措施**：

1. **修改 `analysis_prompt.md`**：在"附录"节的提示词里加入格式强制指令：

```
附录写作规范（强制）：
- 每条来源必须独立成行
- 格式必须为：[来源完整名称](https://完整URL)
- 禁止写法：纯文字URL、模糊来源（如"综合网络信息"）
- 示例：
  [中国政府网 2026年全国两会专题](https://www.gov.cn/zhuanti/2026qglh/)
  [新华社 2026年3月5日：李强作政府工作报告](https://www.xinhuanet.com/politics/2026-03/05/...)
```

2. **修改 `parser.py`**：在解析附录段落时，对不符合 `[text](url)` 格式的来源行输出解析警告（不阻断，但记录在 `ParsedReport` 的 `warnings` 字段里，方便调试）。

3. **修改 `docx_renderer.py`**：确保附录列表项中的 `[text](url)` 被正确渲染为 Word 超链接（目前 `_render_rich_text` 已经支持链接分段，但需要确认附录段落是否走了这条路径）。

**验收标准**：生成的 Word 报告附录中，每条来源都是蓝色下划线可点击超链接。

---

### 2.2 结论部分与前面分析内容重复

**现状**："四、政策权重与空间分析"中的"（三）博弈终局预判"与"六、结论与推导"中的"博弈终局预判"内容高度重叠。

**根因**：8段式模板将"博弈终局预判"同时放在第四部分（空间分析）和第六部分（结论），而提示词没有规定两部分各自的侧重，导致 AI 生成时重复论述。

**修复措施**：

修改 `analysis_prompt.md` 中第六部分"结论与推导"的提示词，明确两部分的分工：

```
第四部分（政策权重与空间分析）中的"博弈终局预判"：
- 只做初步预判：基于当前权重和操作空间，给出走向的初步判断
- 不超过3句话
- 不做详细论证（详细论证留给结论部分）

第六部分（结论与推导）中的"博弈终局预判"：
- 汇总前面所有分析，给出完整的终局预判
- 必须包含：走向判断 + 关键节点 + 失效条件
- 这是报告中最具判断力的部分，需要明确、有主见
```

**验收标准**：第四部分的预判不超过3句，第六部分的预判独立成段、不与前面重复。

---

## 三、P1 问题升级方案

### 3.1 缺乏数据锚点

**目标**：在不硬上数据、不造假的前提下，提升判断的可核验性。

**策略**：分三层增加锚点，按可获取性依次使用。

#### 第一层：精确到可核验颗粒度的来源标注

**做法**：每条事实陈述后面的来源标注，从"媒体名"升级为"媒体名 + 具体页面/日期 + 直接链接"。

修改 `analysis_prompt.md`：

```
事实来源标注规范：
- 格式：[媒体名 完整日期：文章标题](直接链接)
- 示例（好的）：[新华社 2026年3月5日：李强作政府工作报告](https://www.xinhuanet.com/politics/2026-03/05/...)
- 示例（不好的）：（来源：新华社）
- 如果无法直接链接到具体页面，至少给出媒体名 + 具体日期
```

#### 第二层：标注信息缺口

**做法**：对于分析中用到了但无法获取精确数据的关键参数，明确标注"信息缺口"，而不是回避或用模糊表述替代。

在提示词中加入：

```
信息缺口标注规范：
- 如果某个关键数据无法获取，明确标注"【信息缺口】参数X：目前公开信息未披露，分析基于YY假设"
- 示例："【信息缺口】囤积商标的实际清理比例：目前公开信息未披露，本分析基于'修订生效后12个月内清理30%'的假设推演"
- 目的：让读者知道判断的前提条件，也让自己知道哪里需要进一步调研
```

#### 第三层：使用区间而非单点估计

**做法**：对于确实只能估算的数字，用区间表示，并说明区间的上下限依据。

```
数值表述规范：
- 优先使用区间：如"中央财政转移支付约9.8万-10.2万亿元"
- 如果只能用单点估计，标注估计方法和误差范围："约4.5%（按支出法GDP核算，误差范围±0.3%）"
```

**不涉及代码修改**——这三层都是提示词和分析写作规范层面的升级，不需要改 parser 或 renderer。

---

### 3.2 博弈推演从描述升级为阈值推演

**目标**：让博弈分析从"谁会做什么"升级为"在什么条件下谁会做什么，条件变化时行为如何转变"。

**核心概念：阈值推演三要素**

1. **触发条件**（When）：某个主体的行为发生变化的具体条件
2. **临界值**（Threshold）：触发条件中的关键数值或时间点
3. **失效边界**（Breakdown）：当前判断在什么情况下不再成立

**提示词升级方案**：

在 `analysis_prompt.md` 的"政策对象图谱"或"博弈终局预判"部分，加入阈值推演的写作框架：

```
博弈推演写作规范（阈值推演）：

对每个关键主体，分析必须包含：

1. 当前立场（描述性，1-2句）
2. 触发条件（该主体改变立场或行为的具体条件）：
   - 格式："当[条件]达到[临界值]时，[主体]将从[当前行为]转为[新行为]"
   - 示例："当审查周期从12个月降至6个月以下时，品牌企业的防御性注册需求将下降50%以上，其游说投入随之边际递减"
3. 时间窗口（政策执行的最佳/最差时间段）：
   - 格式："[时间点]之前/之后，[现象]，因为[原因]"
   - 示例："修订生效后6-12个月是执行的最佳窗口，因为囤积者的反制能力处于最弱状态（旧商标尚未清理完毕，新申请已受限制）"
4. 失效边界（当前预判的适用上限）：
   - 格式："本预判在[条件]下成立；若[条件]变化，则[预判失效，新判断为……]"
```

**难度评估**：这是本次升级中最难的部分。因为它要求 AI 在生成时进行"准定量"推理，而不仅仅是套用模板。实际效果需要通过多次测试来校准提示词的引导力度。

**折中方案**：如果直接要求阈值推演效果不好，可以先要求"至少给出每个主体行为的时间窗口"，这是一个更简单的阈值（只有时间维度，没有多变量条件），但已经比纯描述性强很多。

---

### 3.3 应对方向从通用升级为针对性

**目标**：让"应对方向"里的建议嵌入本报告的特定分析结论，而不是通用建议。

**做法**：修改提示词，强制应对方向与前面的分析结论建立显式引用关系。

在 `analysis_prompt.md` 的"应对方向"部分：

```
应对方向写作规范：

每条应对建议必须满足：
1. 建议中必须包含本报告中出现的具体概念/参数/时间窗口
2. 禁止出现"应加大投入""应建立机制"等通用表述（除非紧跟具体情境）
3. 格式："[主体]应在[本报告分析出的具体时间窗口/条件]内，[具体操作]，因为[引用本报告的分析结论]"

示例（好的）：
"品牌企业在新法实施后6个月内应完成防御性注册商标的清理——保留核心类别，放弃边缘类别——因为审查提速后核心类别的抢注风险下降，保留边缘类别的防御性注册反而会成为新法下的'恶意囤积'证据。"

示例（不好的）：
"品牌企业应加大研发投入，提升核心竞争力。"
```

**配套修改**：在 `analysis_prompt.md` 里加入一个"应对方向自检清单"，在生成后自检：

```
应对方向自检清单（生成后执行）：
- [ ] 每条建议是否包含了本报告的具体分析结论？
- [ ] 如果把这份建议放到另一个政策的分析报告里，是否仍然读起来合理？如果是，说明建议过于通用，需要重写。
- [ ] 建议中是否给出了具体的时间窗口或操作条件？
```

---

## 四、P2 问题优化方案

### 4.1 段落过长问题

**做法**：在提示词中加入段落长度约束。

```
段落长度规范：
- 正文段落不超过120字（中文）
- 如果一个分析点需要超过120字，分段并用"——"或"首先/其次/最后"等连接词保持连贯
- 引用块（灰色背景）可以稍长，但不超过200字
```

### 4.2 图表与正文叙事分工

**做法**：在 DIAGRAM 的 JSON 描述里，强制要求图表内容与正文形成互补而非重复。

修改 `analysis_prompt.md` 的 DIAGRAM 部分：

```
DIAGRAM 设计原则：
- 图表应展示正文里用文字难以直观表达的因果关系或结构关系
- 禁止：把正文段落重新排版成图（这是重复，不是互补）
- 推荐：用箭头/节点展示因果链、用颜色编码展示利益分布、用时间轴展示政策窗口
- 每个 DIAGRAM 必须回答一个问题：读了正文后，读者还需要看图才能理解的是什么？
```

---

## 五、实施顺序建议

```
第一批（P0，1-2天）：
  修复附录链接格式
  修复结论重复问题
  → 这两项是纯提示词修改，不涉及代码重构，效果立竿见影

第二批（P1，3-5天）：
  升级数据锚点规范（三层）
  升级应对方向针对性规范
  测试阈值推演提示词效果（可能需要多轮校准）
  → 这三项是提示词升级，需要跑多个案例验证效果

第三批（P2，1-2天）：
  段落长度约束
  图表分工规范
  顺手更新 AGENTS.md 案例清单
  → 收尾工作
```

---

## 六、不需要升级的部分

根据对现有报告的评价，以下部分不需要大修：

- **分析框架和8段式结构**：已经很有主见，不需要改
- **四群体分析维度**：框架本身没问题，需要升级的是分析深度（阈值推演），不是框架
- **engine/parser/renderer 的代码架构**：架构设计合理，本次升级不涉及结构性重构
- **可视化方案（PNG+HTML双输出）**：方案扎实，需要优化的是图表内容（上面4.2），不是生成代码

---

## 七、升级后的报告质量目标

| 维度 | 当前 | 升级后目标 |
|---|---|---|
| 数据可核验性 | ★★☆☆☆ | ★★★★☆ |
| 博弈推演深度 | ★★☆☆☆ | ★★★★☆ |
| 应对方向针对性 | ★★☆☆☆ | ★★★★☆ |
| 格式规范性 | ★★★☆☆ | ★★★★★ |
| 阅读节奏 | ★★★☆☆ | ★★★★☆ |
| 判断明确性（已是优点） | ★★★★★ | ★★★★★ |

---

*本方案由 AI 辅助整理，最终实施优先级和具体措施由项目负责人确认。*
