# ----- Python -----
__pycache__/
*.pyc
*.pyo

# ----- 本地工具缓存 / 日志 -----
.reasonix/
reasonix.toml
*.log
rewrite_log_batch*.txt
lo_install.log
/.runtime/

# ----- 本次整治的临时反推脚本（非项目资产）-----
_rebuild_reports.py

# ----- 本地助手记忆（项目级，不入库）-----
.workbuddy/

# ----- 生成的报告产物（docx/pdf/png/html，可由引擎再生，不入库） -----
/reports/

# ----- backend 虚拟环境与运行时数据（源码入库，环境/产物不入库） -----
backend/.venv/
backend/venv/
backend/runtime/

# ----- 清理暂存区（重复/废弃报告的临时回收区，可恢复，不入库） -----
.trash_local/

# ----- 历史孤儿归档（旧 backend 适配器 / 旧 frontend 快照，仅本地留档，不入库） -----
_archived/

# ----- backend 运行时与密钥（源码入库，环境/产物/密钥不入库） -----
backend/.venv/
backend/.venv-broken/
backend/venv/
backend/runtime/
backend/generated/
backend/data/
