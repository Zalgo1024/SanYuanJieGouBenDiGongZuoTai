# 三元结构分析系统 · 上线计划（Demo 推广版）

> 版本：v2（修正 BYOK 认知错误）　·　制定日期：2026-08-31
> 目标：先把项目摆到公网让人试用 → 收集真实反馈 → 稳定后迁国内服务器
> 计费模式：**BYOK**——访客自带 API key，服务器不持有任何 LLM 密钥，你的额度零风险。

---

## 一、总目标与节奏

| 阶段 | 目标 | 预计耗时 | 完成时你得到什么 |
|---|---|---|---|
| 0　准备 | 账号齐活（**无需任何密钥**） | 你 5 分钟 | 具备动手条件 |
| 1　打镜像 | 打包成通用盒子 | 我 30 分钟 | 一个本地能跑的 Docker 镜像 |
| 2　部署 Render | 公网可访问 | 半天 | 一个能打开的网址 |
| 3　验证 | 确认真的能用 | 1 小时 | 上线验证报告（含性能数据） |
| 4　加固 | 文件不丢、数据不丢 | 1 小时 | 报告持久化 + 自动备份 + 保活 |
| 5　推广 | 收集真实反馈 | 时间你定 | 用户反馈清单 |
| 6　迁国内 | 长期稳定运行 | 稳定后再启动 | 国内服务器正式上线 |

---

## 二、每一步：我做什么 / 你做什么

### 阶段 0 · 准备（必须先做完，否则后面全卡住）

**你做（约 15 分钟）**

1. **Render 注册**：https://render.com → 用 GitHub 账号登录（**免费层不用绑卡**）→ 完成后跟我说一声
2. **（可选）HF 用户名改成 `zalgo1024`**：HF Settings → Edit profile → username
   （你目前 HF 是 `zalgo1023`、GitHub 是 `Zalgo1024`，不统一，以后用命令行会绕）

**我做**：等你 OK，立刻开始打镜像。

> ### LLM 密钥：公网部署**不需要你配任何 key**
>
> 系统走的是 **BYOK（Bring Your Own Key）**——访客在自己浏览器里填自己的 key，
> 存自己的浏览器，不经过你的服务器账户。
>
> 你在 8-29 做的 `PUBLIC_MODE=1` 就是这道闸门：开启后 `resolve_config()`
> **彻底关闭"回退到服务器密钥"的兜底逻辑**，服务器 `.env` 里的 key 一律不参与访客请求
> （`llm_settings_store.py:207` 直接返回 `api_key: ""`，测试名就叫
> `test_public_mode_blocks_server_key_fallback`）。
>
> 所以：**你的 DeepSeek key 只留在本地 `backend/.env` 做开发调试，
> 永远不上传、不进镜像、不进 git。** 不存在"额度被访客刷爆"，也不用设消费上限。
>
> ✅ **没有密钥阻塞**。你本地想继续开发调试才需要去 DeepSeek 重生一把 key
> 放进 `backend/.env`——这事可以并行做，不卡部署。

---

### 阶段 1 · 打 Docker 镜像（我 30 分钟）

**我做**

- 写 `Dockerfile`：后端（FastAPI + Python 依赖 + LibreOffice）+ 前端（Next.js 构建产物）
- 本地构建镜像并验证能跑起来
- 跑一次完整流程：生成一份报告（Word + PDF + 利益关系网络图）
- 产出：`Dockerfile` + 本地构建成功的镜像

**你做**：等着就行，不用动手。

**验收**：我给你看本地跑通的证据 + 生成的报告文件。

---

### 阶段 2 · 部署到 Render（半天）

**我做**

- 写 `render.yaml`（一键部署清单：Web Service + PostgreSQL）
- 数据库迁移：SQLite → PostgreSQL（Render 免费版只支持 PG，这一步我来改）
- 出环境变量清单（哪些必填、哪些选填、怎么填）
- 给你图文部署步骤

**你做（约 20 分钟）**

1. 把部署文件推到 GitHub（我给你现成的命令）
2. Render 控制台 → New → Blueprint → 选你的仓库
3. 填环境变量 —— **不填任何 LLM key**，就这几个：

   | 变量 | 值 | 说明 |
   |---|---|---|
   | `PUBLIC_MODE` | `1` | **核心开关**：服务器密钥一律不回退，访客必须自带 key |
   | `HOST` | `0.0.0.0` | **必填**：默认 `127.0.0.1` 只监听本机，容器里不改外部根本访问不到 |
   | `PORT` | `10000` | Render 通常自动注入，一般不用手填 |
   | `SEARCH_ENABLED` | `auto` | 联网搜索；不配 Key 也能走 DuckDuckGo 零 Key 降级 |
   | `DEFAULT_OWNER_NAME` | 留空或中性值 | 默认硬编码"李政恒"，公网多用户下不合适 |

4. 点 Deploy，等 10-15 分钟构建

**验收**：给你一个公网网址，能打开首页。

---

### 阶段 3 · 上线验证（1 小时）

**我做**

- 公网访问测试（我模拟访客把流程走一遍）
- 跑一次完整的报告生成，看公网环境下能不能跑通
- 性能摸底：首屏几秒、生成一份报告多久、内存够不够
- **安全性复查**：确认密钥没出现在任何代码、日志、公开页面里

**你做**

- 用手机流量打开网址，感受真实访问速度
- 发给你信任的 2-3 个人试用，收集第一印象

**产出**：一份《上线验证报告》，含速度数据 + 问题清单 + 可行性结论。

---

### 阶段 4 · 加固（1 小时）

**我做**

- **报告文件持久化（最要紧）**：把 `GENERATED_DIR` 从容器本地磁盘改挂到对象存储
  （Cloudflare R2 免费 10GB，或 Render 付费磁盘 $1/GB/月起）。
  Render 免费层**没有持久磁盘**，不处理的话容器一重启，访客生成的 Word/PDF 全没。
- **自动备份**：每天自动导出数据库，备份到你 GitHub 的**私有**仓库
  （Render 免费 PostgreSQL **90 天后会自动删除**，这是命根子）
- **保活**：配 cron-job.org 每 10 分钟访问一次，防止冷启动休眠
  （免费版 15 分钟无人访问就睡，下次唤醒要 30 秒）
- **BYOK 引导**：首页加一块"怎么拿到免费 API key"的说明，降低访客上手门槛

**产出**：报告不丢、备份能自动跑、网站不休眠、访客知道怎么拿 key。

---

### 阶段 5 · 推广测试（时间你定）

**你做**

- 把网址发给目标用户（小红书 / 闲鱼 / 朋友圈 / 相关社群）
- 收集反馈：能不能打开、快不快、功能好不好用、哪里卡

**我做**

- 你反馈一个我修一个
- 每周汇总一次问题清单

---

### 阶段 6 · 迁移国内（稳定后再启动）

到时候根据当时情况再定，大致是：

- 阿里云 / 腾讯云 / 华为云选型（按当时价格对比）
- **Docker 镜像直接搬过去**（阶段 1 打的镜像就是为这一步准备的）
- 域名购买 + ICP 备案（国内必需，约 20 天）

---

## 三、成本清单

| 阶段 | 费用 |
|---|---|
| 阶段 0 - 5 | **0 元**（Render 免费层；LLM 费用由访客自己的 key 承担，与你无关） |
| 阶段 6（迁国内） | 云服务器 30-100 元/月 + 域名约 50 元/年 |

> **你这边零 LLM 成本**。BYOK 模式下每个访客烧的是自己的额度，
> 你本地开发调试的那点调用量可以忽略不计。

---

## 四、风险清单

| 风险 | 影响 | 对策 |
|---|---|---|
| **报告文件丢失**（最要紧） | 访客生成的 Word/PDF 全没 | Render 免费层没有持久磁盘，`GENERATED_DIR` 必须改挂对象存储（R2 免费 10GB）或付费磁盘 |
| **`HOST` 没改成 `0.0.0.0`** | 部署完外部访问不到，白等 15 分钟 | 环境变量里显式写死 `HOST=0.0.0.0`（默认值 `127.0.0.1` 只监听本机） |
| **数据库 90 天被删** | 用户和报告记录全没 | 每天自动备份到 GitHub 私有仓库 |
| **冷启动 30 秒** | 访客以为网站挂了 | cron-job 保活 + 前端加"正在唤醒"提示 |
| **国内访问慢** | 体验差、访客流失 | 先实测，真不行就转 Render 付费层（$7/月）或提前迁国内 |
| **512MB 内存不够** | 生成报告时崩溃 | 先单并发跑，跑不动再优化或升级 |
| **访客没有 API key** | 进来就用不了，转化率低 | 首页加"哪家有免费额度、怎么拿 key"的引导（9 个服务商里有几家能白嫖） |
| **归属人硬编码"李政恒"** | 公网多用户下显示错乱 | `DEFAULT_OWNER_NAME` 留空或改中性值；长期应从会话/账号体系取 |

---

## 五、需要你拍板的决策点

| # | 决策 | 我的建议 |
|---|---|---|
| 1 | 平台选 Render 还是 HF Space？ | **Render**（国内访问更好、Web 应用对口）。HF 留着以后给海外访客看 |
| 2 | 数据库迁到 PostgreSQL 吗？ | **是**，Render 免费版只支持 PG，迁移的活我来干 |
| 3 | 现在就买域名吗？ | **先不买**，用 Render 免费域名（xxx.onrender.com）测试，稳定了再买 |
| 4 | 报告文件存哪？ | 先挂 **Cloudflare R2**（免费 10GB，够 demo 用）；嫌麻烦就先用 Render 付费磁盘 $1/GB/月 |
| 5 | 访客没 key 用不了，会不会劝退？ | 首页加引导最好；真想零门槛，以后可以考虑"每日 N 次免费额度"用你自己的 key 池（但那就是你掏钱了，demo 阶段不建议） |

---

## 六、现在就开始：你先做这两件事

1. **Render 注册**：https://render.com → GitHub 账号登录（不用绑卡）
2. **（本地开发用，不卡部署）** DeepSeek 重生密钥：platform.deepseek.com → 创建新 key → 放进 `backend/.env`

第 1 件做完跟我说一声，我立刻开始打镜像。

---

*本计划随执行进展更新。*
